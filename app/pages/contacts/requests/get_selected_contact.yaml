# @cascade: MongoDB Request - Selected Contact Retrieval
# AI Context: Single document retrieval by ID with company lookup
# MCP Integration: Validate MongoDBFindOne request schema and lookup operations
# Patterns: Document retrieval by ID, relationship population via lookup
# Dependencies: connections.yaml contacts collection, URL query parameters

id: get_selected_contact
type: MongoDBFindOne
connectionId: contacts
payload:
  _id:
    _url_query: id
properties:
  query:
    _id:
      _payload: _id
