# @context
# Name: Companies Selector
# Description: MongoDB request for companies selector options
# Agents Context: MongoDB request for companies selector options

id: companies_selector
type: MongoDBAggregation
connectionId: companies
properties:
  pipeline:
    - $project:
        label: $name
        value: $_id
    - $sort:
        label: 1
