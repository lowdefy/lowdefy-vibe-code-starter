# @cascade: Lowdefy Shared Component - Change Stamp
# AI Context: Reusable timestamp and user tracking for audit trails
# MCP Integration: Validate shared component schema and timestamp operations
# Patterns: Audit trail creation, user tracking, timestamp generation
# Dependencies: User authentication system, used across all CRUD operations

timestamp:
  _date: now
user:
  name:
    _user: profile.name
  id:
    _user: id
